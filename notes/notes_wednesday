in:

 https://applevac.atlassian.net/wiki/spaces/DOCKER
    
  

~/docker-volumes/Databases:>(docker exec -i alg-postgres psql -U alg) < ./ari-CI.sql



1) Install Docker
2) Add Ed’s certificate   http://fileserv01.local/~alg/registry.p12 password: alg
3) clone the dev-environment repo
4) Setup script that has sudo:
    1)map ed's temporary fileserver and registry in the hosts folder
5) Run setup script (no sudo)
    1) copies wars and creates folders
    2) untags the DB volume to ~/docker-volumes/postgres

6) configure docker to use more cores and memory  





